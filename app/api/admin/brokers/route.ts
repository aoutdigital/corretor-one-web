import { NextResponse } from "next/server";
import { authorizeAdmin } from "@/lib/admin/authorization";

export async function GET(request:Request){const access=await authorizeAdmin(request,"brokers.read");if(!access.ok)return NextResponse.json({ok:false,error:{message:access.message}},{status:access.status});const url=new URL(request.url);const status=url.searchParams.get("status");let query=access.db.from("profiles").select("id,primeiro_nome,sobrenome,email,nickname,creci_uf,creci_numero,creci_sufixo,creci_aprovacao,creci_documento_midia_id,status,created_at").order("created_at",{ascending:false}).limit(100);if(status==="PENDENTE")query=query.eq("creci_aprovacao",false);const result=await query;if(result.error)return NextResponse.json({ok:false,error:{message:result.error.message}},{status:500});return NextResponse.json({ok:true,data:result.data??[]})}
