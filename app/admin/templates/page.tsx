"use client";
import Link from "next/link";import{useEffect,useState}from"react";import{AdminShell}from"@/app/admin/_components/admin-shell";import{apiFetchWithAuth}from"@/lib/client/auth-api";
type Template={id:string;nome:string;renderer_key:string;version:number;formatos:string[];ativo:boolean;updated_at:string};
export default function TemplatesPage(){const[items,setItems]=useState<Template[]>([]);const[error,setError]=useState<string|null>(null);useEffect(()=>{apiFetchWithAuth<Template[]>("/api/admin/templates").then((r)=>r.ok?setItems(r.data):setError(r.error))},[]);return <AdminShell title="Templates"><p className="mb-5 text-sm text-slate-500">Ajustes visuais controlados, com rascunho e publicação versionada.</p>{error?<p className="rounded-xl bg-red-50 p-3 text-red-700">{error}</p>:null}<div className="grid gap-4 md:grid-cols-2">{items.map((item)=><Link href={`/admin/templates/${item.id}`} key={item.id} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm transition hover:border-slate-400"><div className="flex justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--grey-olive)]">{item.renderer_key}</p><h2 className="mt-2 text-xl font-bold">{item.nome}</h2><p className="mt-2 text-sm text-slate-500">{item.formatos.join(" · ")}</p></div><span className="h-fit rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">v{item.version}</span></div></Link>)}</div></AdminShell>}
